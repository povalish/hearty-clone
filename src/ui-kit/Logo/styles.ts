import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  base: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'flex-end',
  },

  text: {
    fontWeight: 'bold',
    fontSize: 32,
  },

  icon: {
    marginHorizontal: 2,
  },
});
